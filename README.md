
# Week 1 Python & Git

- `hello.py`: prints greeting and date, echoes user input
- `sum_evens.py`: sums even numbers from 1..n
- `topk.py`: returns top-k numbers above a threshold
- `botk.py`: returns bot-k numbers below a threshold
- `safecounter.py`: SafeCounter class with inc/dec/reset
- `pd.py`): same with pandas
